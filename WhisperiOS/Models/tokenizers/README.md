Whisper tokenizer files

Tokenizer files are not committed to git.

To download the tokenizer files used by this app:

1) From repo root, run:
   ./scripts/bootstrap_ios_models.sh
2) Rebuild the app.

Target folder:
  - WhisperiOS/Models/tokenizers/openai-whisper-tiny
