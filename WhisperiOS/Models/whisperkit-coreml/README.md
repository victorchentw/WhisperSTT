WhisperKit CoreML model

Model binaries are not committed to git.

To download the WhisperKit tiny model used by this app:

1) From repo root, run:
   ./scripts/bootstrap_ios_models.sh
2) Rebuild the app.

Target folder:
  - WhisperiOS/Models/whisperkit-coreml/openai_whisper-tiny
